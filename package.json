{
  "name": "pgaotcdn_cf_pages",
  "version": "1.0.0",
  "description": "A file upload service for Cloudflare Pages",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'No build step required for Cloudflare Pages'",
    "deploy-workers": "wrangler deploy",
    "deploy-pages": "echo 'Deploy to Cloudflare Pages using dashboard or GitHub integration'"
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^4.27.0"
  }
}
